@{
    ViewData["Title"] = "Access Denied";
}

<div class="container-narrow">
    <div class="card">
        <div class="card-body">
            <h4 class="mb-2">Access Denied</h4>
            <div class="text-muted mb-3">You don't have permission to access this page.</div>
            <a asp-controller="Account" asp-action="Login" class="btn btn-primary">Go to Login</a>
        </div>
    </div>
</div>
